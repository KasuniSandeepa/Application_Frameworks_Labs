
/*
const UUID = require('uuid');

//Here we import the functions that we are created in the database file
const {getAll, getById, removeById, save, update} = require('../dal/posts.dao');


let createPost = async (name , description) =>{
    let post = {
        id: UUID.v4(),
        name : name,
        description : description,
        postedDate: new Date()
    }

    //We need to wait until the data is loaded here
    return await save(post);
};

let getPosts = () =>{
    return 0;
}

const getPost = id=>{
    return 0;
}

module.exports ={
    createPost,
    getPosts,
    getPost

}


*/
